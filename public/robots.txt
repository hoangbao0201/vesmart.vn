# Cho phép bot lập chỉ mục nội dung công khai
User-agent: *
Allow: /

# Không index trang quản trị (nếu có)
Disallow: /admin/

# Sitemap — dùng URL production; có thể đổi domain khi deploy
Sitemap: https://vesmart.vn/sitemap.xml
